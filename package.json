{
  "name": "neu",
  "private": true,
  "description": "Neuroscience Engineering Universe — project catalogue website",
  "scripts": {
    "validate": "npx -y ajv-cli validate -s database.schema.json -d database.json --spec=draft2020",
    "serve": "npx -y serve ."
  }
}
